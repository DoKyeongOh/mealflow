# Used with @ActiveProfiles("test"). Same MySQL settings as the main app (see application.properties + infra/docker-compose.yml).
# OAuth placeholders so the Security OAuth2 client registry initializes; tests do not call Google.
GOOGLE_CLIENT_ID=test-google-client-id
GOOGLE_CLIENT_SECRET=test-google-client-secret
